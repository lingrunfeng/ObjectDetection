# 数据集标注说明

## 📦 类别信息

本项目包含4种积木类别：
- `red_rectangle` - 红色长方形积木
- `red_triangle` - 红色三角形积木
- `red_squre` - 红色正方体积木
- `blue_squre` - 蓝色正方体积木

## 🚀 快速开始标注

### ✅ 环境已配置完成

**labelme 已安装并修复兼容性问题，可以直接使用！**

如果不确定环境是否正常，可以运行：
```bash
cd /home/student26/ObjectDetection/dataset
./test_labelme.sh
```

### ⚠️ 关于标注工具

由于 labelImg 在当前环境（Python 3.12 + Ubuntu 24.04）存在兼容性问题，**已配置好 labelme**（更稳定，兼容性更好）。

### 方案A: 使用 Labelme（推荐）⭐

**labelme 已安装并配置完成，可以直接使用！**

**开始标注：**

**标注训练集：**
```bash
cd /home/student26/ObjectDetection/dataset
./start_labelme_train.sh
```

**标注验证集：**
```bash
cd /home/student26/ObjectDetection/dataset
./start_labelme_val.sh
```

**3. 标注完成后转换为 YOLO 格式：**

Labelme 会生成 JSON 格式的标注文件，需要转换为 YOLO 格式（TXT）：

```bash
# 转换训练集
python3 labelme_to_yolo.py lables/train lables/train images/train

# 转换验证集
python3 labelme_to_yolo.py lables/val lables/val images/val
```

### 方案B: 使用 LabelImg（如果方案A不可用）

**1. 安装 LabelImg：**
```bash
pip3 install --break-system-packages labelImg setuptools
```

**2. 开始标注：**
```bash
cd /home/student26/ObjectDetection/dataset
./start_labeling_train.sh  # 可能会遇到兼容性问题
```

⚠️ **注意**：如果遇到 `TypeError` 或 `Segmentation fault` 错误，请使用方案A（labelme）。

## 📋 标注步骤（快速参考）

1. **启动 LabelImg**
2. **选择格式**：点击左侧 **"YOLO"** 按钮（重要！）
3. **打开图片文件夹**：
   - 训练集：`dataset/images/train`
   - 验证集：`dataset/images/val`
4. **设置保存位置**：
   - 训练集标注：`dataset/lables/train`
   - 验证集标注：`dataset/lables/val`
5. **开始标注**：
   - 按 `W` 键画框
   - 输入类别名称（见上方类别信息）
   - 按 `D` 键下一张（会自动保存）

## 📁 数据集结构

```
dataset/
├── images/
│   ├── train/          # 14张训练图片
│   └── val/            # 5张验证图片
├── lables/             # 标注文件目录（注意拼写）
│   ├── train/          # 训练集标注（.txt格式）
│   └── val/            # 验证集标注（.txt格式）
├── classes.txt         # 类别列表文件
├── LABELING_GUIDE.md         # 详细标注指南（labelImg）
├── ALTERNATIVE_LABELING.md   # 替代标注工具说明（推荐）
├── labelme_to_yolo.py        # Labelme 转 YOLO 格式脚本
├── start_labelme_train.sh    # 启动 labelme 标注训练集（推荐）⭐
├── start_labelme_val.sh      # 启动 labelme 标注验证集（推荐）⭐
├── start_labeling_train.sh   # 启动 labelImg 标注训练集
└── start_labeling_val.sh     # 启动 labelImg 标注验证集
```

## ⚠️ 注意事项

1. **文件夹名称**：注意标注文件夹名是 `lables`（不是标准的 `labels`），这是项目的命名约定
2. **格式选择**：务必选择 **YOLO 格式**，不要选择 PascalVOC 格式
3. **类别名称**：标注时输入的类别名称必须与 `classes.txt` 中的完全一致
4. **标注完整性**：确保每张图片中的可见积木都被标注

## 📖 详细说明

更多详细的标注说明和快捷键，请查看 `LABELING_GUIDE.md` 文件。

